# Web framework and ASGI server
fastapi==0.136.1
uvicorn==0.47.0

# Database (async SQLAlchemy against Supabase Postgres) and migrations
SQLAlchemy==2.0.49
asyncpg==0.31.0
greenlet==3.5.0
alembic==1.18.4

# Settings and validation
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0
python-dotenv==1.2.2

# Authentication (owned by this app, not Supabase Auth)
bcrypt==5.0.0
PyJWT==2.13.0
python-multipart==0.0.29

# 3D generation pipeline: call TRELLIS on Colab Gradio and store the .glb
gradio_client==2.5.0
supabase==2.31.0

# Tooling: linting and tests
ruff==0.15.15
pytest==9.0.3
pytest-asyncio==1.4.0
httpx==0.28.1
